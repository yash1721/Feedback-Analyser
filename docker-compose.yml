services:
  feedbackiq-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    environment:
      DATABASE_URL: postgresql+psycopg://feedbackiq:feedbackiq@feedbackiq-db:5432/feedbackiq
      LOCAL_STORAGE_DIR: /app/.local_storage
      QDRANT_URL: http://feedbackiq-qdrant:6333
    depends_on:
      - feedbackiq-db
      - feedbackiq-redis
      - feedbackiq-qdrant

  feedbackiq-worker:
    build: .
    env_file:
      - .env
    command: celery -A app.workers.celery_app worker --loglevel=info --pool=solo
    volumes:
      - .:/app
    environment:
      DATABASE_URL: postgresql+psycopg://feedbackiq:feedbackiq@feedbackiq-db:5432/feedbackiq
      LOCAL_STORAGE_DIR: /app/.local_storage
      REDIS_URL: redis://feedbackiq-redis:6379/0
      CELERY_BROKER_URL: redis://feedbackiq-redis:6379/0
      CELERY_RESULT_BACKEND: redis://feedbackiq-redis:6379/1
      QDRANT_URL: http://feedbackiq-qdrant:6333
    depends_on:
      - feedbackiq-db
      - feedbackiq-redis
      - feedbackiq-qdrant

  feedbackiq-db:
    image: postgres:16
    environment:
      POSTGRES_DB: feedbackiq
      POSTGRES_USER: feedbackiq
      POSTGRES_PASSWORD: feedbackiq
    ports:
      - "5432:5432"
    volumes:
      - feedbackiq-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U feedbackiq -d feedbackiq"]
      interval: 5s
      timeout: 5s
      retries: 10

  feedbackiq-redis:
    image: redis:7
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  feedbackiq-qdrant:
    image: qdrant/qdrant:v1.12.6
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - feedbackiq-qdrant-data:/qdrant/storage
    healthcheck:
      test: ["CMD-SHELL", "bash -c ':> /dev/tcp/127.0.0.1/6333'"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  feedbackiq-postgres-data:
  feedbackiq-qdrant-data:
